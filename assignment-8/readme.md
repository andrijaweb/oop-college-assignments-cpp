**Zа апстрактну Funkciju, може да се израчуна:

реална вредност у тачки x (f(x)),
да се са стандардног улаза прочитају параметри функције,
да се испише алгебарски облик функције
PrirodniLogaritam је Funkcija аlog(bx), подразумевано log(x) која се исписује у облику alog(bx), где су a и b вредности параметара функције.

Polinom је Funkcija anxn+an-1xn-1+...+a1x+a0 која се ствара задатог реда и са коефицијентима једнаким 1.

може да се дохвати ред полинома (оператор +)
да се приступи појединим коефицијентима (оператор [])
да се испише у облику p(x)[an,an-1,...,a1,a0], где су ai вредности коефицијената полинома.
Написати главни програм на програмском језику C++ који проверава исправност претходних класа.**